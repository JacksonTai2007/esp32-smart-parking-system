# 引脚分配表（Pinout）

代码中的唯一权威定义在 `firmware/parking-system/config/Pins.h`，
本表与其保持一致；修改任意一处必须同步另一处和 README。

> 本期方案：红外车位传感器做车辆识别 + 车位管理 + 按时长计费（默认 2 车位），
> 并加入智能入场 + 自动分配车位引导（入口触摸感应 → 自动分配空闲车位并引导，
> `ENABLE_ENTRY_GUIDE`）与火灾报警（火焰传感器 → 警报 + 风扇联动，
> `ENABLE_FIRE_ALARM`）。**本项目不做 RFID 刷卡、不用舵机闸机**；
> HC-SR04 入口超声波未纳入。

| 功能 | 模块 | ESP32 引脚 | 备注 |
| --- | --- | --- | --- |
| I2C SDA | OLED SSD1306 | GPIO 21 | I2C |
| I2C SCL | OLED SSD1306 | GPIO 22 | I2C |
| 蜂鸣器 | Buzzer | GPIO 14 | 数字输出 |
| 车位 1 | 红外传感器 | GPIO 32 | 数字输入 |
| 车位 2 | 红外传感器 | GPIO 33 | 数字输入 |
| 车位 3 | 红外传感器 | GPIO 34 | 输入专用 |
| 车位 4 | 红外传感器 | GPIO 35 | 输入专用 |
| 入口感应（智能入场） | TTP223 触摸模块 DO | GPIO 25 | 数字输入（内部下拉，未接不误触发） |
| 火灾报警 | 火焰传感器 DO | GPIO 27 | 数字输入（内部上拉，未接不误报） |
| 风扇联动 | 继电器/MOSFET | GPIO 16 | 数字输出，火警时自动启动 |
| 降雨提示 | 雨滴传感器 DO | GPIO 17 | 数字输入（内部上拉，未接不误报） |
| 碰撞告警 | 震动传感器 DO | GPIO 18 | 数字输入（内部上拉，未接不误报） |
| 烟雾检测 | MQ 烟雾模块 | GPIO 36 | Phase 2 预留，模拟输入 |

## 注意事项（接线前必读）

1. **OLED 使用 3.3V 供电**，SDA/SCL 接 GPIO 21/22；I2C 地址多数为 0x3C，
   少数 0x3D（改 `config/Settings.h` 的 `OLED_I2C_ADDRESS`）。
2. **外接电源时 ESP32、各传感器必须共地（GND 连通）**，
   否则信号电平没有共同参考，行为随机异常。
3. **GPIO 34、35、36（及 39）是输入专用脚**：不能作为输出，
   也没有内部上拉/下拉。车位红外模块自带推挽输出，可直接接入；
   如换成开漏输出的模块需外接上拉电阻。
   车位脚 32/33 已启用内部上/下拉：**未接模块时车位显示"空闲"而非"全满"**；
   若扩展到 34/35 则必须接上模块（无内部拉，悬空读数不定）。
4. **启动敏感（strapping）引脚 GPIO 0/2/5/12/15**：本期方案均未使用，
   扩展功能时优先避开这些脚。
5. **实际接线后需根据模块的高低电平逻辑调整 `config/Settings.h`**：
   - 红外车位模块：常见为"检测到物体输出 LOW"（`SLOT_OCCUPIED_LEVEL = LOW`），
     若你的模块相反，改为 `HIGH`
   - 蜂鸣器模块：默认高电平触发（`BUZZER_ACTIVE_LEVEL = HIGH`），
     低电平触发的模块改为 `LOW`
6. 只接 2~3 个车位时，把 `Settings.h` 的 `TOTAL_PARKING_SLOTS` 改成实际数量，
   按顺序占用 GPIO 32→33→34 引脚。
7. GPIO 36 为 ADC1 通道，Phase 2 的 MQ 烟雾模块 AO 输出接它做模拟采样；
   使用 Wi-Fi 时 ADC2 不可用，因此模拟输入只规划在 ADC1 引脚上。
8. **入口触摸感应（智能入场）**：TTP223 的 DO 接 GPIO 25，拍一下感应片即识别为
   "车辆到达入口"，自动分配空闲车位并在网页 / OLED 引导（触发电平
   `ENTRY_ACTIVE_LEVEL`，TTP223 默认按下输出 HIGH）。触摸感应在模拟模式下同样
   有效；网页「车辆到达入口」按钮 / `POST /api/entry` 是等价触发。
9. **火焰 / 雨滴 / 震动 / 风扇继电器的触发电平**都在 `config/Settings.h` 可调
   （`FLAME_ACTIVE_LEVEL`、`RAIN_ACTIVE_LEVEL`、`IMPACT_ACTIVE_LEVEL`、
   `FAN_ACTIVE_LEVEL`），这类比较器模块常见"触发时输出 LOW"。全部输入引脚
   都启用了内部上/下拉，**不接模块也不会误报/误入场**。课堂演示可用红外遥控器对准火焰传感器按键安全触发火警
   （火焰传感器本质是红外光敏管），无需明火。
